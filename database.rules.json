{
  "rules": {
    "predictions": {
      ".read": true,
      ".write": false,
      "$predictionId": {
        ".validate": "newData.hasChildren(['timestamp', 'results', 'imageUrl'])"
      }
    }
  }
} 